name: ondex_support
description: OnDex — qo'llab-quvvatlash (aloqa ma'lumotlari va restoran ↔ OnDex admin chati). Restoran va admin panellari AYNI kodni ishlatadi.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  # Chatga rasm tanlash oynasi — ikkala panel ham shu versiyani ishlatadi.
  file_picker: ^8.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
