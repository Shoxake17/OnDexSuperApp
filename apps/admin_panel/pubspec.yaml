name: chust_admin
description: ChustApp — Superadmin panel
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  ondex_core:
    path: ../../packages/ondex_core
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.0
  google_maps_flutter: ^2.10.0
  geolocator: ^13.0.1
  web: ^1.1.0
  file_picker: ^8.1.3

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
